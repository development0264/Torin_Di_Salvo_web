import { NumberFormatterDirectiveDirective } from './number-formatter-directive.directive';

describe('NumberFormatterDirectiveDirective', () => {
  it('should create an instance', () => {
    const directive = new NumberFormatterDirectiveDirective();
    expect(directive).toBeTruthy();
  });
});
