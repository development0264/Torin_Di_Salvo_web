import { DollarsignDirective } from './dollarsign.directive';

describe('DollarsignDirective', () => {
  it('should create an instance', () => {
    const directive = new DollarsignDirective();
    expect(directive).toBeTruthy();
  });
});
